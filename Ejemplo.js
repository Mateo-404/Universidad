function a(dice) {
    let sum = 0;
    dice.includes();
    /*
    for (let i = 0; i < 5; i++){
        let contador = 0;
        // Triplete
        if (dice[i] == dice[i + 1] && dice[i] == dice[i + 2]) {
            // Puntaje
            dice[i] == 1 ? sum += 1000 : sum += (dice[i] * 100);
            // Cancelación de Triplete
            i += 2;
        }else{
            // Puntaje comun
            dice[i] == 1 ? sum += 100 : dice[i] == 5 ? sum += 50 : 0;
        }
    }
    return sum;
    */
}
let prueba = [1, 1, 1, 3, 1]; // 1000

console.log(a(prueba));
